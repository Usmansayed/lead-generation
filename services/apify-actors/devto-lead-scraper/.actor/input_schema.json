{
    "title": "Dev.to Lead Scraper",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "keywords": {
            "title": "Keywords to Search For",
            "type": "array",
            "description": "Lead generation keywords to search Dev.to for",
            "editor": "stringList",
            "placeholderKey": "keyword",
            "placeholderValue": "e.g., freelance developer"
        },
        "maxArticles": {
            "title": "Maximum Articles",
            "type": "integer",
            "description": "Maximum number of articles to scrape",
            "default": 50,
            "minimum": 1,
            "maximum": 500
        },
        "minDelayMs": {
            "title": "Minimum Delay (ms)",
            "type": "integer",
            "description": "Minimum delay between requests in milliseconds",
            "default": 2000,
            "minimum": 500,
            "maximum": 10000
        },
        "maxDelayMs": {
            "title": "Maximum Delay (ms)",
            "type": "integer",
            "description": "Maximum delay between requests in milliseconds",
            "default": 4000,
            "minimum": 1000,
            "maximum": 20000
        }
    },
    "required": ["keywords"]
}
